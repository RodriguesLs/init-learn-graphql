type Profile {
  id: Int
  name: String
}

enum UserStatus {
  ACTIVE
  BLOCK
  INACTIVE
} 

type User {
  id: ID
  name: String!
  email: String!
  age: Int
  payment: Float
  vip: Boolean
  profile: Profile
  status: UserStatus
}